runtime: nodejs18
build:
  commands:
    - npm ci
    - npm run build
deploy:
  package:
    include:
      - dist/**
      - server.js
      - package.json
      - package-lock.json
  start:
    command: npm start
